apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth
  namespace: argocd
spec:
  project: default
  source:
    chart: githubhelm/auth
    repoURL: ghcr.io/belalkhaledmohameed
    targetRevision: 0.1.0
    helm:
      releaseName: auth
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true

